<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Events_GSS.Views.NotificationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Events_GSS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:models="using:ChatAndEvents.Data.EventsData.Models"
    xmlns:converters="using:Events_GSS.Converters"
    x:Name="Root">

    <Page.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:BoolToInverseVisibilityConverter x:Key="BoolToInverseVisibilityConverter"/>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
    </Page.Resources>
    
    <Grid RowDefinitions="Auto,*" Padding="24" RowSpacing="16">

        <TextBlock
            Grid.Row="0"
            Text="Notifications"
            Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1">
            <ProgressRing
                IsActive="{Binding IsLoading}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding IsLoading}"/>

            <TextBlock
                Text="{Binding ErrorMessage}"
                Foreground="Red"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

            <StackPanel Visibility="{Binding IsLoading, Converter={StaticResource BoolToInverseVisibilityConverter}}">
                <TextBlock
                    Text="No notifications yet."
                    HorizontalAlignment="Center"
                    Margin="0,24"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Visibility="{Binding Notifications.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>

                <ListView ItemsSource="{Binding Notifications}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Notification">
                            <Border
                                Padding="16,12"
                                Margin="0,4"
                                CornerRadius="8"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1">
                                <Grid ColumnDefinitions="*,Auto">
                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <TextBlock
                                            Text="{Binding Title}"
                                            Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock
                                            Text="{Binding Description}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            TextWrapping="Wrap"/>
                                        <TextBlock
                                            Text="{Binding CreatedAt}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            FontSize="12"/>
                                    </StackPanel>
                                    <Button
                                        Grid.Column="1"
                                        Content="Delete"
                                        Tag="{Binding}"
                                        Click="DeleteButton_Click"
                                        VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
